# Private overlay network

Creates a network and sandbox, attaches the sandbox, verifies network membership, and detaches it. The example destroys both resources when it finishes. See [main.rs](main.rs) for the code.

From the repository root:

```sh
export CREATEOS_API_KEY="your-api-key"
cargo run --example network
```
