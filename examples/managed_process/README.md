# Managed process lifecycle

Exercises process input, output, waiting, PTY resizing, and termination in a sandbox. The example destroys the sandbox when it finishes. See [main.rs](main.rs) for the code.

From the repository root:

```sh
export CREATEOS_API_KEY="your-api-key"
cargo run --example managed_process
```
